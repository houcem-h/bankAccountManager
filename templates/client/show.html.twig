{% extends 'base.html.twig' %}

{% block title %}Liste des clients{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    <br>
    <a href="{{ path('client') }}" role="button" class="btn btn-outline-primary"><i class="fas fa-step-backward"></i> Retour à la liste des clients</a>
    <br><br>
    <div class="card" style="width: 18rem;">
        <img class="card-img-top" src="http://monumentfamilydentistry.com/wp-content/uploads/2015/11/user-placeholder-300x295.png" alt="Card image cap">
        <div class="card-body">
            <h5 class="card-title">{{ client.prenom }} {{ client.nom }}</h5>
            <p class="card-text">
                <div class="row"><i class="fas fa-calendar-day">  {{ client.dateNaissance|date("Y/m/d") }}</i></div><br>
                <div class="row"><i class="fas fa-phone">  {{ client.tel }}</i></div><br>
                <div class="row"><i class="fas fa-map-marked-alt">  {{ client.adresse }}</i></div><br>

            </p>
            <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
    </div>  
{% endblock %}
